# Developer placeholder. The web application writes inventory.generated.yml
# into each isolated cluster workspace and always invokes it explicitly.
[loadbalancer]

[control_plane]

[worker]

[k8s_cluster:children]
control_plane
worker
